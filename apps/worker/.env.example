# The worker runs as an independent OS process separate from the API server,
# so it does not inherit or share the API's runtime environment variables.
# It requires its own .env file to configure database and redis connections.

DATABASE_URL=""
REDIS_URL=""
TOKEN_ENCRYPTION_KEY=""
