$kind: http-request
url: "{{baseUrl}}/api/v1/auth/login"
method: POST
body:
  type: json
  content: "{\r

    \    \"email\": \"bakery@test.com\",\r

    \  \"password\": \"test1234\"\r

    }"
auth:
  type: bearer
scripts:
  - type: afterResponse
    code: "const data = pm.response.json();\r

      if (data.accessToken) pm.collectionVariables.set(\"accessToken\",
      data.accessToken);\r

      if (data.refreshToken) pm.collectionVariables.set(\"refreshToken\",
      data.refreshToken);"
    language: text/javascript
order: 1789633780193
